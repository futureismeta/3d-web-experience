import enableWs from "express-ws";

export const websocketDirectoryChangeListener = (
    app: enableWs.Application,
    { directory, websocketPath }: { directory: string; websocketPath: string }
) => {
    // Add your implementation for websocketDirectoryChangeListener here
};